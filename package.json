{
  "name": "omnistack-9-backend",
  "version": "1.0.0",
  "main": "src/server.js",
  "license": "MIT",
  "scripts": {
    "npm:install": "npm install -g tsc ts-node-dev && npm install",
    "yarn:install": "yarn global add tsc ts-node-dev && yarn install",
    "build": "tsc",
    "prod": "tsc && node ./dist/server.js",
    "run:dev": "ts-node-dev --respawn --transpileOnly ./src/server.ts",
    "run:prod": "node ./dist/server.js"
  },
  "dependencies": {
    "@types/express": "^4.17.1",
    "@types/mongoose": "^5.5.19",
    "@types/multer": "^1.3.10",
    "dotenv-flow": "^3.1.0",
    "express": "^4.17.1",
    "mongoose": "^5.7.3",
    "multer": "^1.4.2"
  },
  "devDependencies": {
    "ts-node-dev": "^1.0.0-pre.43"
  }
}
